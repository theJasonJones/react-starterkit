import React from "react";
import ReactDOM from "react-dom";

import "./index.css";

const App = () => (
  <div>You can start me up, I'll never stop. Never, never never...</div>
);

ReactDOM.render(<App />, document.getElementById("app"));
